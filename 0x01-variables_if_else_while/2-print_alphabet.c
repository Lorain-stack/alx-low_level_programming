#include <stdio.h>

/**
* main - Entry Point
* Description: This program will assign a random number to the variable
* Return: Always 0 (Success)
*/

int main(void)
{
	char c;

	c = 'a';
	while
		(c <= 'z') {
			putchar(c);
			c++;
		}
	putchar('\n');
	return (0);
}
